server:
  error:
    include-message: always
    include-binding-errors: always
  compression:
     enabled: true
---
 management:
   endpoints:
    web:
     base-path: /
     path-mapping.healh: health
    enable-y-default: false
   endpoint:
    health:
     enabled: true
     show-details: always
     
 logging:
  level:
   root: info